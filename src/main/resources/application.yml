spring:
  profiles:
    include:
      - web
      - mybatis
    active: dev
---
spring:
  profiles: dev
mybatis-plus:
  global-config:
    db-config:
      db-type: mysql
---
spring:
  profiles: test
